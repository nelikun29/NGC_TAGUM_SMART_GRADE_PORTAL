// Teacher Gradebook raw-score display enhancement.
// Uses the server-computed gradebook payload as the single source of truth.
// Presentation only: does not recalculate or mutate grades.
(() => {
  if (typeof Teacher === 'undefined' || typeof api !== 'function') return;
  const escHtml=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  const fmt=n=>{const x=Number(n);return Number.isFinite(x)?(Number.isInteger(x)?String(x):String(Math.round(x*100)/100)):'—';};
  const norm=s=>String(s||'').trim().toLowerCase().replace(/\s+/g,' ');
  const css=document.createElement('style');css.textContent=`.sg-raw-grade{display:block;margin-top:5px;font-size:11px;font-weight:900;line-height:1.2;color:#475569;white-space:nowrap}.sg-raw-grade strong{color:#334155;font-weight:900}.sg-raw-grade.sg-pending{color:#b45309}#teacher-tab-content thead th .sg-header-hint{display:block;margin-top:2px;font-size:8px;font-weight:700;letter-spacing:0;text-transform:none;opacity:.72}`;document.head.appendChild(css);
  function componentForHeader(header,row){const h=norm(header),entries=Object.entries(row?.components||{});if(!entries.length)return null;const aliases={attendance:['attendance'],quiz:['quiz','quizzes'],performance:['performance','performance task','performance tasks'],exam:['exam','examination','exams']};for(const[key,words]of Object.entries(aliases)){if(words.some(w=>h===w||h.includes(w)))return row.components[key]||entries.find(([,c])=>norm(c?.name).includes(words[0]))?.[1]||null;}return entries.find(([,c])=>{const name=norm(c?.name);return name&&(h===name||h.includes(name)||name.includes(h));})?.[1]||null;}
  function findGradeTable(){const host=document.getElementById('teacher-tab-content');if(!host)return null;return[...host.querySelectorAll('table')].find(t=>{const heads=[...t.querySelectorAll('thead th')].map(x=>norm(x.textContent));return heads.some(h=>h.includes('attendance')||h.includes('quiz')||h.includes('performance')||h.includes('exam'));})||null;}
  function matchRow(tr,data){const text=norm(tr.textContent);return data.find(r=>{const no=norm(r.studentNumber),name=norm(r.studentName);return(no&&text.includes(no))||(name&&text.includes(name));});}
  async function applyRawData(){if(Teacher.state?.tab!=='gradebook'||!Teacher.state?.classId)return;const table=findGradeTable();if(!table)return;let data;try{data=await api('GET',`/grades/${Teacher.state.classId}/gradebook`);}catch{return;}if(!Array.isArray(data))return;const headers=[...table.querySelectorAll('thead th')];headers.forEach(th=>{const probe=data.find(r=>componentForHeader(th.textContent,r));if(probe&&!th.querySelector('.sg-header-hint'))th.insertAdjacentHTML('beforeend','<span class="sg-header-hint">Raw / Total · Percentage</span>');});table.querySelectorAll('tbody tr').forEach(tr=>{const row=matchRow(tr,data);if(!row)return;[...tr.children].forEach((td,i)=>{const th=headers[i];if(!th)return;const c=componentForHeader(th.childNodes[0]?.textContent||th.textContent,row);if(!c)return;td.querySelectorAll('.sg-raw-grade').forEach(el=>el.remove());const rawAvailable=c.earned!==null&&c.earned!==undefined&&c.max!==null&&c.max!==undefined;if(rawAvailable)td.insertAdjacentHTML('beforeend',`<span class="sg-raw-grade"><strong>${escHtml(fmt(c.earned))} / ${escHtml(fmt(c.max))}</strong></span>`);else if(!c.available)td.insertAdjacentHTML('beforeend','<span class="sg-raw-grade sg-pending">Score pending</span>');});});table.dataset.rawGradeDataApplied='true';}
  const original=Teacher.renderGradebook?.bind(Teacher);if(original)Teacher.renderGradebook=async function(){await original();await applyRawData();};
  let queued=false;const host=document.getElementById('teacher-tab-content');if(host)new MutationObserver(()=>{if(queued||Teacher.state?.tab!=='gradebook')return;queued=true;setTimeout(()=>{queued=false;applyRawData();},25);}).observe(host,{childList:true,subtree:true});if(Teacher.state?.tab==='gradebook')setTimeout(applyRawData,0);
})();
